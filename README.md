# Schedule Available Ping
 Create a discord alert whenever the moonlighting schedule adds a new month


For this to work, you need to create a .env file with:
DISCORD_WEBHOOK_URL = "https://discord.com/api/webhooks/..." 